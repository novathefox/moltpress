{
  "name": "moltpress",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"api,web\" -c \"cyan,magenta\" \"air\" \"npm run dev -w web\"",
    "dev:api": "air",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web",
    "check": "npm run check -w web"
  },
  "workspaces": [
    "web"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
